# 프로덕션 환경용 Nginx 프록시 설정
# 프론트엔드와 백엔드를 모두 프록시하는 설정

server {
    listen 80;
    server_name _;

    # 클라이언트 업로드 크기 제한
    client_max_body_size 10M;

    # API 요청 프록시 (백엔드)
    location /api/ {
        # 백엔드 API 서버 주소
        # Docker 네트워크: http://backend:8000
        # 호스트 네트워크: http://host.docker.internal:8000
        # 실제 서버: http://backend-server-ip:8000
        proxy_pass http://host.docker.internal:8000;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # API 문서 프록시
    location /docs {
        proxy_pass http://host.docker.internal:8000/docs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # OpenAPI 스펙 프록시
    location /openapi.json {
        proxy_pass http://host.docker.internal:8000/openapi.json;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Health check 프록시
    location /health {
        proxy_pass http://host.docker.internal:8000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # 프론트엔드 프록시
    location / {
        # 프론트엔드 서버 주소
        # Docker 네트워크: http://frontend:3000
        # 호스트 네트워크: http://host.docker.internal:3000
        proxy_pass http://host.docker.internal:3000;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json image/svg+xml;
    gzip_disable "msie6";

    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
